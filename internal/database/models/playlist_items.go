// Code generated by SQLBoiler 4.19.7 (https://github.com/aarondl/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/aarondl/sqlboiler/v4/boil"
	"github.com/aarondl/sqlboiler/v4/queries"
	"github.com/aarondl/sqlboiler/v4/queries/qm"
	"github.com/aarondl/sqlboiler/v4/queries/qmhelper"
	"github.com/aarondl/strmangle"
	"github.com/friendsofgo/errors"
)

// PlaylistItem is an object representing the database table.
type PlaylistItem struct {
	ID         string    `boil:"id" json:"id" toml:"id" yaml:"id"`
	CreatedAt  time.Time `boil:"created_at" json:"created_at" toml:"created_at" yaml:"created_at"`
	UpdatedAt  time.Time `boil:"updated_at" json:"updated_at" toml:"updated_at" yaml:"updated_at"`
	PlaylistID string    `boil:"playlist_id" json:"playlist_id" toml:"playlist_id" yaml:"playlist_id"`
	VideoID    string    `boil:"video_id" json:"video_id" toml:"video_id" yaml:"video_id"`
	Position   int64     `boil:"position" json:"position" toml:"position" yaml:"position"`

	R *playlistItemR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L playlistItemL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var PlaylistItemColumns = struct {
	ID         string
	CreatedAt  string
	UpdatedAt  string
	PlaylistID string
	VideoID    string
	Position   string
}{
	ID:         "id",
	CreatedAt:  "created_at",
	UpdatedAt:  "updated_at",
	PlaylistID: "playlist_id",
	VideoID:    "video_id",
	Position:   "position",
}

var PlaylistItemTableColumns = struct {
	ID         string
	CreatedAt  string
	UpdatedAt  string
	PlaylistID string
	VideoID    string
	Position   string
}{
	ID:         "playlist_items.id",
	CreatedAt:  "playlist_items.created_at",
	UpdatedAt:  "playlist_items.updated_at",
	PlaylistID: "playlist_items.playlist_id",
	VideoID:    "playlist_items.video_id",
	Position:   "playlist_items.position",
}

// Generated where

var PlaylistItemWhere = struct {
	ID         whereHelperstring
	CreatedAt  whereHelpertime_Time
	UpdatedAt  whereHelpertime_Time
	PlaylistID whereHelperstring
	VideoID    whereHelperstring
	Position   whereHelperint64
}{
	ID:         whereHelperstring{field: "\"playlist_items\".\"id\""},
	CreatedAt:  whereHelpertime_Time{field: "\"playlist_items\".\"created_at\""},
	UpdatedAt:  whereHelpertime_Time{field: "\"playlist_items\".\"updated_at\""},
	PlaylistID: whereHelperstring{field: "\"playlist_items\".\"playlist_id\""},
	VideoID:    whereHelperstring{field: "\"playlist_items\".\"video_id\""},
	Position:   whereHelperint64{field: "\"playlist_items\".\"position\""},
}

// PlaylistItemRels is where relationship names are stored.
var PlaylistItemRels = struct {
	Video    string
	Playlist string
}{
	Video:    "Video",
	Playlist: "Playlist",
}

// playlistItemR is where relationships are stored.
type playlistItemR struct {
	Video    *Video    `boil:"Video" json:"Video" toml:"Video" yaml:"Video"`
	Playlist *Playlist `boil:"Playlist" json:"Playlist" toml:"Playlist" yaml:"Playlist"`
}

// NewStruct creates a new relationship struct
func (*playlistItemR) NewStruct() *playlistItemR {
	return &playlistItemR{}
}

func (o *PlaylistItem) GetVideo() *Video {
	if o == nil {
		return nil
	}

	return o.R.GetVideo()
}

func (r *playlistItemR) GetVideo() *Video {
	if r == nil {
		return nil
	}

	return r.Video
}

func (o *PlaylistItem) GetPlaylist() *Playlist {
	if o == nil {
		return nil
	}

	return o.R.GetPlaylist()
}

func (r *playlistItemR) GetPlaylist() *Playlist {
	if r == nil {
		return nil
	}

	return r.Playlist
}

// playlistItemL is where Load methods for each relationship are stored.
type playlistItemL struct{}

var (
	playlistItemAllColumns            = []string{"id", "created_at", "updated_at", "playlist_id", "video_id", "position"}
	playlistItemColumnsWithoutDefault = []string{"id", "created_at", "updated_at", "playlist_id", "video_id"}
	playlistItemColumnsWithDefault    = []string{"position"}
	playlistItemPrimaryKeyColumns     = []string{"id"}
	playlistItemGeneratedColumns      = []string{}
)

type (
	// PlaylistItemSlice is an alias for a slice of pointers to PlaylistItem.
	// This should almost always be used instead of []PlaylistItem.
	PlaylistItemSlice []*PlaylistItem
	// PlaylistItemHook is the signature for custom PlaylistItem hook methods
	PlaylistItemHook func(context.Context, boil.ContextExecutor, *PlaylistItem) error

	playlistItemQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	playlistItemType                 = reflect.TypeOf(&PlaylistItem{})
	playlistItemMapping              = queries.MakeStructMapping(playlistItemType)
	playlistItemPrimaryKeyMapping, _ = queries.BindMapping(playlistItemType, playlistItemMapping, playlistItemPrimaryKeyColumns)
	playlistItemInsertCacheMut       sync.RWMutex
	playlistItemInsertCache          = make(map[string]insertCache)
	playlistItemUpdateCacheMut       sync.RWMutex
	playlistItemUpdateCache          = make(map[string]updateCache)
	playlistItemUpsertCacheMut       sync.RWMutex
	playlistItemUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var playlistItemAfterSelectMu sync.Mutex
var playlistItemAfterSelectHooks []PlaylistItemHook

var playlistItemBeforeInsertMu sync.Mutex
var playlistItemBeforeInsertHooks []PlaylistItemHook
var playlistItemAfterInsertMu sync.Mutex
var playlistItemAfterInsertHooks []PlaylistItemHook

var playlistItemBeforeUpdateMu sync.Mutex
var playlistItemBeforeUpdateHooks []PlaylistItemHook
var playlistItemAfterUpdateMu sync.Mutex
var playlistItemAfterUpdateHooks []PlaylistItemHook

var playlistItemBeforeDeleteMu sync.Mutex
var playlistItemBeforeDeleteHooks []PlaylistItemHook
var playlistItemAfterDeleteMu sync.Mutex
var playlistItemAfterDeleteHooks []PlaylistItemHook

var playlistItemBeforeUpsertMu sync.Mutex
var playlistItemBeforeUpsertHooks []PlaylistItemHook
var playlistItemAfterUpsertMu sync.Mutex
var playlistItemAfterUpsertHooks []PlaylistItemHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *PlaylistItem) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range playlistItemAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *PlaylistItem) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range playlistItemBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *PlaylistItem) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range playlistItemAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *PlaylistItem) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range playlistItemBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *PlaylistItem) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range playlistItemAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *PlaylistItem) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range playlistItemBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *PlaylistItem) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range playlistItemAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *PlaylistItem) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range playlistItemBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *PlaylistItem) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range playlistItemAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddPlaylistItemHook registers your hook function for all future operations.
func AddPlaylistItemHook(hookPoint boil.HookPoint, playlistItemHook PlaylistItemHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		playlistItemAfterSelectMu.Lock()
		playlistItemAfterSelectHooks = append(playlistItemAfterSelectHooks, playlistItemHook)
		playlistItemAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		playlistItemBeforeInsertMu.Lock()
		playlistItemBeforeInsertHooks = append(playlistItemBeforeInsertHooks, playlistItemHook)
		playlistItemBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		playlistItemAfterInsertMu.Lock()
		playlistItemAfterInsertHooks = append(playlistItemAfterInsertHooks, playlistItemHook)
		playlistItemAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		playlistItemBeforeUpdateMu.Lock()
		playlistItemBeforeUpdateHooks = append(playlistItemBeforeUpdateHooks, playlistItemHook)
		playlistItemBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		playlistItemAfterUpdateMu.Lock()
		playlistItemAfterUpdateHooks = append(playlistItemAfterUpdateHooks, playlistItemHook)
		playlistItemAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		playlistItemBeforeDeleteMu.Lock()
		playlistItemBeforeDeleteHooks = append(playlistItemBeforeDeleteHooks, playlistItemHook)
		playlistItemBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		playlistItemAfterDeleteMu.Lock()
		playlistItemAfterDeleteHooks = append(playlistItemAfterDeleteHooks, playlistItemHook)
		playlistItemAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		playlistItemBeforeUpsertMu.Lock()
		playlistItemBeforeUpsertHooks = append(playlistItemBeforeUpsertHooks, playlistItemHook)
		playlistItemBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		playlistItemAfterUpsertMu.Lock()
		playlistItemAfterUpsertHooks = append(playlistItemAfterUpsertHooks, playlistItemHook)
		playlistItemAfterUpsertMu.Unlock()
	}
}

// One returns a single playlistItem record from the query.
func (q playlistItemQuery) One(ctx context.Context, exec boil.ContextExecutor) (*PlaylistItem, error) {
	o := &PlaylistItem{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for playlist_items")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all PlaylistItem records from the query.
func (q playlistItemQuery) All(ctx context.Context, exec boil.ContextExecutor) (PlaylistItemSlice, error) {
	var o []*PlaylistItem

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to PlaylistItem slice")
	}

	if len(playlistItemAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all PlaylistItem records in the query.
func (q playlistItemQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count playlist_items rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q playlistItemQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if playlist_items exists")
	}

	return count > 0, nil
}

// Video pointed to by the foreign key.
func (o *PlaylistItem) Video(mods ...qm.QueryMod) videoQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.VideoID),
	}

	queryMods = append(queryMods, mods...)

	return Videos(queryMods...)
}

// Playlist pointed to by the foreign key.
func (o *PlaylistItem) Playlist(mods ...qm.QueryMod) playlistQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.PlaylistID),
	}

	queryMods = append(queryMods, mods...)

	return Playlists(queryMods...)
}

// LoadVideo allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (playlistItemL) LoadVideo(ctx context.Context, e boil.ContextExecutor, singular bool, maybePlaylistItem any, mods queries.Applicator) error {
	var slice []*PlaylistItem
	var object *PlaylistItem

	if singular {
		var ok bool
		object, ok = maybePlaylistItem.(*PlaylistItem)
		if !ok {
			object = new(PlaylistItem)
			ok = queries.SetFromEmbeddedStruct(&object, &maybePlaylistItem)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybePlaylistItem))
			}
		}
	} else {
		s, ok := maybePlaylistItem.(*[]*PlaylistItem)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybePlaylistItem)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybePlaylistItem))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &playlistItemR{}
		}
		args[object.VideoID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &playlistItemR{}
			}

			args[obj.VideoID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`videos`),
		qm.WhereIn(`videos.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Video")
	}

	var resultSlice []*Video
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Video")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for videos")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for videos")
	}

	if len(videoAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Video = foreign
		if foreign.R == nil {
			foreign.R = &videoR{}
		}
		foreign.R.PlaylistItems = append(foreign.R.PlaylistItems, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.VideoID == foreign.ID {
				local.R.Video = foreign
				if foreign.R == nil {
					foreign.R = &videoR{}
				}
				foreign.R.PlaylistItems = append(foreign.R.PlaylistItems, local)
				break
			}
		}
	}

	return nil
}

// LoadPlaylist allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (playlistItemL) LoadPlaylist(ctx context.Context, e boil.ContextExecutor, singular bool, maybePlaylistItem any, mods queries.Applicator) error {
	var slice []*PlaylistItem
	var object *PlaylistItem

	if singular {
		var ok bool
		object, ok = maybePlaylistItem.(*PlaylistItem)
		if !ok {
			object = new(PlaylistItem)
			ok = queries.SetFromEmbeddedStruct(&object, &maybePlaylistItem)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybePlaylistItem))
			}
		}
	} else {
		s, ok := maybePlaylistItem.(*[]*PlaylistItem)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybePlaylistItem)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybePlaylistItem))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &playlistItemR{}
		}
		args[object.PlaylistID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &playlistItemR{}
			}

			args[obj.PlaylistID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`playlists`),
		qm.WhereIn(`playlists.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Playlist")
	}

	var resultSlice []*Playlist
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Playlist")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for playlists")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for playlists")
	}

	if len(playlistAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Playlist = foreign
		if foreign.R == nil {
			foreign.R = &playlistR{}
		}
		foreign.R.PlaylistItems = append(foreign.R.PlaylistItems, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.PlaylistID == foreign.ID {
				local.R.Playlist = foreign
				if foreign.R == nil {
					foreign.R = &playlistR{}
				}
				foreign.R.PlaylistItems = append(foreign.R.PlaylistItems, local)
				break
			}
		}
	}

	return nil
}

// SetVideo of the playlistItem to the related item.
// Sets o.R.Video to related.
// Adds o to related.R.PlaylistItems.
func (o *PlaylistItem) SetVideo(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Video) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"playlist_items\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 0, []string{"video_id"}),
		strmangle.WhereClause("\"", "\"", 0, playlistItemPrimaryKeyColumns),
	)
	values := []any{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.VideoID = related.ID
	if o.R == nil {
		o.R = &playlistItemR{
			Video: related,
		}
	} else {
		o.R.Video = related
	}

	if related.R == nil {
		related.R = &videoR{
			PlaylistItems: PlaylistItemSlice{o},
		}
	} else {
		related.R.PlaylistItems = append(related.R.PlaylistItems, o)
	}

	return nil
}

// SetPlaylist of the playlistItem to the related item.
// Sets o.R.Playlist to related.
// Adds o to related.R.PlaylistItems.
func (o *PlaylistItem) SetPlaylist(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Playlist) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"playlist_items\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 0, []string{"playlist_id"}),
		strmangle.WhereClause("\"", "\"", 0, playlistItemPrimaryKeyColumns),
	)
	values := []any{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.PlaylistID = related.ID
	if o.R == nil {
		o.R = &playlistItemR{
			Playlist: related,
		}
	} else {
		o.R.Playlist = related
	}

	if related.R == nil {
		related.R = &playlistR{
			PlaylistItems: PlaylistItemSlice{o},
		}
	} else {
		related.R.PlaylistItems = append(related.R.PlaylistItems, o)
	}

	return nil
}

// PlaylistItems retrieves all the records using an executor.
func PlaylistItems(mods ...qm.QueryMod) playlistItemQuery {
	mods = append(mods, qm.From("\"playlist_items\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"playlist_items\".*"})
	}

	return playlistItemQuery{q}
}

// FindPlaylistItem retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindPlaylistItem(ctx context.Context, exec boil.ContextExecutor, iD string, selectCols ...string) (*PlaylistItem, error) {
	playlistItemObj := &PlaylistItem{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"playlist_items\" where \"id\"=?", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, playlistItemObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from playlist_items")
	}

	if err = playlistItemObj.doAfterSelectHooks(ctx, exec); err != nil {
		return playlistItemObj, err
	}

	return playlistItemObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *PlaylistItem) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no playlist_items provided for insertion")
	}

	var err error
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		if o.UpdatedAt.IsZero() {
			o.UpdatedAt = currTime
		}
	}

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(playlistItemColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	playlistItemInsertCacheMut.RLock()
	cache, cached := playlistItemInsertCache[key]
	playlistItemInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			playlistItemAllColumns,
			playlistItemColumnsWithDefault,
			playlistItemColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(playlistItemType, playlistItemMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(playlistItemType, playlistItemMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"playlist_items\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"playlist_items\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into playlist_items")
	}

	if !cached {
		playlistItemInsertCacheMut.Lock()
		playlistItemInsertCache[key] = cache
		playlistItemInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the PlaylistItem.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *PlaylistItem) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		o.UpdatedAt = currTime
	}

	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	playlistItemUpdateCacheMut.RLock()
	cache, cached := playlistItemUpdateCache[key]
	playlistItemUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			playlistItemAllColumns,
			playlistItemPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update playlist_items, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"playlist_items\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 0, wl),
			strmangle.WhereClause("\"", "\"", 0, playlistItemPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(playlistItemType, playlistItemMapping, append(wl, playlistItemPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update playlist_items row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for playlist_items")
	}

	if !cached {
		playlistItemUpdateCacheMut.Lock()
		playlistItemUpdateCache[key] = cache
		playlistItemUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q playlistItemQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for playlist_items")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for playlist_items")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o PlaylistItemSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]any, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), playlistItemPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"playlist_items\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, playlistItemPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in playlistItem slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all playlistItem")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *PlaylistItem) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no playlist_items provided for upsert")
	}
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		o.UpdatedAt = currTime
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(playlistItemColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	playlistItemUpsertCacheMut.RLock()
	cache, cached := playlistItemUpsertCache[key]
	playlistItemUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			playlistItemAllColumns,
			playlistItemColumnsWithDefault,
			playlistItemColumnsWithoutDefault,
			nzDefaults,
		)
		update := updateColumns.UpdateColumnSet(
			playlistItemAllColumns,
			playlistItemPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("models: unable to upsert playlist_items, could not build update column list")
		}

		ret := strmangle.SetComplement(playlistItemAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 {
			conflict = make([]string, len(playlistItemPrimaryKeyColumns))
			copy(conflict, playlistItemPrimaryKeyColumns)
		}
		cache.query = buildUpsertQuerySQLite(dialect, "\"playlist_items\"", updateOnConflict, ret, update, conflict, insert)

		cache.valueMapping, err = queries.BindMapping(playlistItemType, playlistItemMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(playlistItemType, playlistItemMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []any
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "models: unable to upsert playlist_items")
	}

	if !cached {
		playlistItemUpsertCacheMut.Lock()
		playlistItemUpsertCache[key] = cache
		playlistItemUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single PlaylistItem record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *PlaylistItem) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no PlaylistItem provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), playlistItemPrimaryKeyMapping)
	sql := "DELETE FROM \"playlist_items\" WHERE \"id\"=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from playlist_items")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for playlist_items")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q playlistItemQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no playlistItemQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from playlist_items")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for playlist_items")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o PlaylistItemSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(playlistItemBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []any
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), playlistItemPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"playlist_items\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, playlistItemPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from playlistItem slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for playlist_items")
	}

	if len(playlistItemAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *PlaylistItem) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindPlaylistItem(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *PlaylistItemSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := PlaylistItemSlice{}
	var args []any
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), playlistItemPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"playlist_items\".* FROM \"playlist_items\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, playlistItemPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in PlaylistItemSlice")
	}

	*o = slice

	return nil
}

// PlaylistItemExists checks if the PlaylistItem row exists.
func PlaylistItemExists(ctx context.Context, exec boil.ContextExecutor, iD string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"playlist_items\" where \"id\"=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if playlist_items exists")
	}

	return exists, nil
}

// Exists checks if the PlaylistItem row exists.
func (o *PlaylistItem) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return PlaylistItemExists(ctx, exec, o.ID)
}
