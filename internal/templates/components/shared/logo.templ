package shared

import "strings"

templ Logo(class ...string) {
	<div class={ "text-center font-bold text-primary underline underline-offset-4 pointer-events-none", strings.Join(class, " ") }>
		feedlr
	</div>
}
