package shared

import "strings"

templ Logo(class ...string) {
	<div class={ "pointer-events-none inline-flex items-center font-display font-semibold text-xl mt-0.25", strings.Join(class, " ") }>
		feedlr
	</div>
}
