package shared

import "strings"

templ Logo(class ...string) {
	<div class={ "pointer-events-none inline-flex items-center font-display text-[1.38rem] leading-none font-semibold tracking-[-0.02em] text-text-primary md:text-[1.48rem]", strings.Join(class, " ") }>
		feedlr
	</div>
}
